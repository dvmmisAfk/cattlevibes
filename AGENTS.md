<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# MANDATORY DESIGN & INTERACTION RULES

**CRITICAL:** Before taking any action or writing any code on any user prompt, ALWAYS thoroughly review and adhere to:
1. `DESIGN.md` (and `HOME_PAGE_DESIGN.md` / `SECONDARY_PAGES_DESIGN.md` where applicable) - Master specification for branding, colors (Pebble, Yam `#ea9216`, High Tide `#313841`, Cadet Blue `#3a4750`), typography (Manrope / Inter), geometry, layout, component architecture, and the 21-product veterinary catalog.
2. `APPLE_DESIGN_SKILL.md` (and `.agents/skills/apple-design/SKILL.md`) - Principles of fluid interaction, interruptible spring physics, response on pointer-down, velocity handoff, translucent materials, and accessibility.
